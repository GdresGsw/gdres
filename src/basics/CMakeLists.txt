add_library(wbasics "")

add_subdirectory(parts)
add_subdirectory(log)
add_subdirectory(fiber)
add_subdirectory(thread)
add_subdirectory(timer)
add_subdirectory(modulmgr)


# add_subdirectory(WSocket)
# add_subdirectory(WMemory)
